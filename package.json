{
  "name": "parcel-prueba",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-pug": "2.8.3",
    "@parcel/transformer-sass": "2.8.3",
    "parcel": "^2.8.3",
    "push-dir": "^0.4.1"
  },
  "scripts": {
    "dev": "parcel src/*.pug",
    "build": "parcel build src/*.pug -d dist",
    "push-gh-pages": "yarn push-dir --dir-dist --branh-gh-pages --cleanup --verbose"
  }
}